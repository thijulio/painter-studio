# libs/ — código compartilhado (puro, testável)

- `shared/color-engine` — RGB/HSL/Lab, delta-E, extração/quantização, harmonias, mistura.
- `shared/grid-engine` — tamanhos de tela, grelha, tiling A4.
- `shared/types` — tipos compartilhados.
- `shared/api-client` — cliente tipado p/ as functions.
- `frontend/ui` — componentes visuais compartilhados.
