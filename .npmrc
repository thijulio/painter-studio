# Escopo @thijulio → GitHub Packages (design system + dev-tooling).
@thijulio:registry=https://npm.pkg.github.com/

# Auth: o ~/.npmrc global já injeta _authToken=${NODE_AUTH_TOKEN} (vem do ~/.zshrc via `gh`).
# Em CI, defina NODE_AUTH_TOKEN (ou GITHUB_TOKEN).
