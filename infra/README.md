# infra/ — infraestrutura como código

- `s3/` — bucket de imagens (privado, lifecycle, CORS e política IAM mínima).
- `netlify/` — `netlify.toml`, redirects, env.
- `database/` — Neon Postgres schema and transactional migrations.
